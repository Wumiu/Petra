# Changelog

本项目的所有重要变更都会记录在此文件中。

## [Unreleased]

- 合并组员提交 74b85c6「upgrade riichi mahjong visuals and interactions」：立直麻将表现层重做（原创 SVG 牌面 tileAssets、手牌/摸牌/牌河/副露/宝牌指示/牌山牌背统一视觉）、引擎与视图交互改进、游戏内表情反应可抢占节流（reactNow force）、新增 tests/riichi-state.test.js 与 scripts/riichi-ui-smoke.mjs、scripts/riichi-round2-acceptance.mjs 验收脚本

- 右键菜单支持多级子菜单：此前渲染器只处理一层，写在子项里的 submenu 会被静默忽略（"跟随音乐"点了没反应就是这个原因）；现在可逐级手风琴展开，并新增第二级以下的层级样式
- 🎵 歌词气泡：识别当前正在播放的歌曲并在桌宠旁时不时弹出当前歌词行（右键菜单 → 交互 → 跟随音乐 → 歌词气泡）
- 🈶 歌词翻译：可在「跟随音乐 → 歌词翻译」开关；译文来自网易云 tlyric（与原文同时间戳），按行对齐后**显示在原文下方**（字号更小、颜色更淡），不是接在原文后面；来源没有译文时就只显示原文
- 新增网易云歌词来源（优先）并保留 LRCLIB 兜底：实测用户曲库（日系同人/vocaloid/华语）LRCLIB 命中 0/7、网易云命中 7/7 且均带 LRC 时间戳；两个来源统一成同一数据形状，前端按标题/艺人/时长挑选（标题完全不符直接弃用，避免出错误歌词）
- 过滤网易云歌词里的制作信息行（作曲/编曲/混音等），避免把"作曲 : xxx"当成第一句歌词
- 修复歌词比人声慢半拍（整首歌固定延迟）：根因是时钟以"SMTC 轮询发现换歌"的时刻为 0 秒，而轮询本身晚了几百毫秒。现在①换歌瞬间若处于两首之间的静音空隙，则把 0 秒锚定到"第一声响起"的那一刻（限时 1.5 秒，安静前奏不会锚错，播放中途才发现换歌则不锚）②SMTC 轮询 1000ms → 500ms ③提前量 120ms → 200ms
- 歌词气泡改为跟随模型：贴在模型上方（放不下翻到模型下方），并与小助手气泡自动叠层避让
- 纯音乐/曲库没有的歌：静默不跟唱（不再弹"没有歌词"提示），只保留左上角歌名气泡
- 修复"时不时只微微眨一只眼"：跟唱时跳过音乐驱动的随机单眼眨眼（0.35 秒单眼脉冲被缓动吃掉大半，看起来像眨眼不完全），改由自然眨眼（双眼、完整）负责
- 歌词跟唱延迟修复：轮询间隔 500ms → 200ms，并加入 200ms 提前量；同时支持 LRC 的 [offset:±ms] 整体平移
- 提升歌词获取健壮性：Rust 侧"精确匹配 → 搜索"两次请求（间隔 1.5 秒，避免扎堆触发限流），失败后由前端按 20s/45s/90s 退避静默重试
- 修复歌词"永远取不到"的三个问题：① LRCLIB 突发 503 被当成"没有歌词"并写入 6 小时负缓存（现在接口失败不缓存，且后台静默重试）；② 取歌词只请求一次（现在 /api/get 重试一次再退 /api/search，间隔 2 秒避免撞限流）；③ 无法区分"这首歌本来没歌词"与"接口坏了"（现在区分 未收录 / 纯音乐 / 接口失败 三种提示）
- 新增 src-tauri/src/media.rs：通过 Windows 系统媒体会话（SMTC）读取歌名/艺人/播放状态，不要求播放器窗口可见、不装插件、不改播放器设置
- 新增 src/music/*：LRC 解析、歌词时钟（换歌归零/暂停停表/长静音判定单曲循环/疑似跳转标记失准）、LRCLIB 在线歌词 + 本地缓存、歌词气泡渲染
- 歌词接口每次请求结果写入应用日志（字节数 / 是否含同步歌词 / 失败原因），便于定位
- 新增歌词测试脚本 npm run test:music：31 项单元测试（LRC 解析、行定位、候选挑选、时钟状态机）
- 歌词经 Rust 侧 fetch_lyrics 命令走 LRCLIB（免费无 key，含 LRC 时间戳），复用现有 PowerShell 请求模式，不新增 Rust HTTP 依赖

- 🎮 小游戏模块：右键菜单「🎮 小游戏」入口，全窗口覆盖层 + 可扩展注册表（games/host.ts）
- 🀄 双人立直麻将：与桌宠一对一，支持碰/明杠/暗杠/立直/宝牌/听牌提示/番数算点（无符简化）/流局听牌罚符；桌宠 AI 按手牌效率打牌、按役牌决定碰杠；和了/被和/流局实时情绪反馈
- 麻将规则与界面完全本地运行，**零 token 消耗**（API 余额不足或断网时照样能陪伴）
- 助手 API 报错友好化：402 余额不足改为清晰提示并引导去玩小游戏，另覆盖 401/429/超时/404 模型错误
- 新增麻将测试脚本 npm run test:riichi：14 项规则单元测试 + 12 场机器人在线对局模拟（校验不卡死与分数守恒）
- 修复麻将引擎 request() 先刷新 UI 后挂等待者导致的同步应答丢失死锁
- 气泡情绪着色：AI 回复按情绪（开心/难过/生气/惊讶/害羞/疲惫/爱意/担心）自动换背景色
- 心情驱动主动关怀：心情低谷自动触发安慰型问候；熬夜关怀时段拓宽到 23:00-5:00
- token 统计升级：白名单提供商回传服务端真实 usage（含 DeepSeek 前缀缓存命中），设置面板展示缓存命中量
- 情感反馈系统：本地规则识别用户/AI 情绪 → 驱动角色表情与动作（零 token），新增心情状态机（互动增减、随时间回归基线）
- 人格系统：小助手设置新增「对用户的称呼」，system prompt 注入紧凑环境上下文（时间/陪伴时长）
- token 优化：主聊天记忆按场景召回（≤6 条）、system 记忆注入截断（≤20 条）、主动问候与抽卡点评禁用工具定义、问候不污染对话历史、聊天抽卡省一次文案生成
- 修复记忆提取硬编码 DeepSeek 端点导致其他提供商静默失败的问题
- 重写 README，完善使用说明
- 新增开源许可与致谢、商标与图片版权免责声明

## [v0.1.7] - 2026-08-21

### Fixed

- 修复 Windows 系统代理 / Clash 环境下 updater 无法联网的问题（自动使用系统代理）
- 为 updater 检查增加合理超时（12s），避免网络不可达时长时间等待
- 移除已失效的 mirror.ghproxy.com fallback
- 保留并增强 updater 并发检查与错误处理（single-flight、手动取消自动）

### Security

- 继续使用 Tauri 官方 updater 与正式数字签名验证
- 不提供未验证安装包 fallback

## [v0.1.6] - 2026-08-20

- 自动更新安全迁移：接入 Tauri 官方 updater 插件（内置 minisign 签名验证）
- 移除旧的无验签下载执行链（http_get / http_download / write_update_installer / launch_update_installer）
- 更新 updater 信任链：新的签名密钥对，从 v0.1.6 起更新必须验签
- 新增 GitHub Actions Release workflow（tauri-action 自动签名 + 生成 latest.json）

## [v0.1.5] - 2026-08-20

- 新增自定义更新管理器（多镜像源 + 下载进度），`release/latest.json` 更新清单

## [v0.1.4] - 2026-08-19

- 区域穿透：交互区域改为精确判定（InteractiveRegion 替代整窗透明）
- 信息版：新增信息面板（桌面、天气、待办提醒）
- 修复 showUpdateBubble XSS 漏洞（改 textContent）
- 修复 dev 模式资源路径错误、模型路径候选
- 行为引擎重构：clearTarget 递归 bug 修复、Magic Numbers 提取

## [v0.1.3] - 2026-08-15

- 模型管理：已导入的 PSD 模型现在可在「模型设置」中直接删除
- 安全删除：删除前二次确认；删除当前正在使用的模型时自动回退到内置模型
- 存储安全：Rust 后端限制删除范围，仅允许操作应用数据目录中的已导入 PSD
- 修复：PSD 文件名允许包含点号；导入失败时给出提示并回退内置模型

## [v0.1.2] - 2026-08-15

- 新增动作系统（14 个动作 / 动作池分级）
- BPM 音乐跟随与节拍眨眼
- 拖拽摆动
- 修复动作回落、眨眼镜像、帧率相关阻尼等问题

## [v0.1.1] - 2026-08-13

- 新增检查更新提示：启动自动检查 + 菜单手动检查，点击气泡打开浏览器下载 Release
- README 顶部新增快速获取引导（普通用户走 Releases，开发者走源码）

## [v0.1.0] - 2026-08-13

- 首个版本：Live2D 桌面桌宠（Tauri + Anime2.5DRig PSD 自动装配）
- 行为引擎与交互体验优化
- 修复多项问题并重构核心模块
- 第一轮修复：安全启动应用 / Shell 校验 / 引擎优化
- 第二轮修复：SMTP 授权码加密、反馈邮件直发、清理占位角色残留、音乐摆动标注未完善
- 项目文档与图标更新
