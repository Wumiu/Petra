运行即执行 `src/main.ts` 中的 `boot()`，需浏览器环境存在 `<div id="stage">` 容器；首次加载会尝试读取 `localStorage.live2d-pet-psd` 与 `/models/manifest.json` 决定模型来源，并通过 Tauri IPC 调用 `read_psd`、`save_psd`、`work_area_at`、`cursor_pos`、`drag_start/end`、`hide_pet`、`restart_app`、`quit_app`、`set_autostart`、`get_autostart`、`list_models`、`clear_pet_target`、`set_pet_target[_speed]` 等 Rust 命令。